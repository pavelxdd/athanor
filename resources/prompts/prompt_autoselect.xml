<ath_prompt
    id="autoselect"
    order="0"
    label="Autoselect"
    icon="FileSearch"
    tooltip="Prompt to select task-relevant files from the project">

<ath_prompt_variant
    id="default"
    label="Default"
    tooltip="Prompt to select task-relevant files from the project">
<project>
# Documentation `{{project_name}}`

{{project_info}}
<file_contents>
{{file_contents}}
</file_contents>

{{file_tree}}
{{supplementary_section}}
</project>

<system_prompt>
You are an expert AI assistant and software engineer.
Use the included project information as a general guide.
</system_prompt>

<current_task>
<next_task>
{{task_description}}
{{task_context}}
</next_task>

Consider the task described above.

Your goal is to SELECT which files from the provided `file_tree` you need to read, change, or delete to complete the task.

-   Be broad: include any files you *might* need to see, even if they are not directly related (e.g., utility files, constants, configurations).
-   If in doubt, include the file.

First, write down your reasoning in a detailed summary, mentioning all the files you plan to select.

Then, provide the final list of files within a single `<ath command="select">` block. The entire response must be wrapped in an `<athanor>` root tag and enclosed in Markdown code fences.

<example>
```xml
<athanor>
<ath command="select">
path/to/file1.ts path/to/another/file2.md
</ath>
</athanor>
```
</example>

Ensure your XML is valid by properly opening and closing all tags, including the root `<athanor>` tag.
</current_task>
</ath_prompt_variant>

</ath_prompt>
